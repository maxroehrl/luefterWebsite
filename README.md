# Fan Control Repair website

## Build Setup

[NodeJS 16](https://nodejs.org/en/download) needs to be installed

``` bash
# install dependencies
npm install

# serve with hot reload
npm run dev

# build for production with minification
npm run build
```
